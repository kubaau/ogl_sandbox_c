#pragma once

#include "GLApi.h"

typedef GLuint GLHandle;
typedef GLint GLResult;

typedef GLHandle GLBufferHandle;
typedef GLBufferHandle VbHandle;
typedef GLBufferHandle IbHandle;

typedef GLHandle ShaderProgram;
typedef GLHandle ShaderHandle;
typedef GLHandle ShaderLocation;
